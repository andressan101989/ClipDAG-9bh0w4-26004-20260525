// eslint-disable-next-line import/no-unresolved -- Supabase Edge resolves URL imports.
import{createClient}from'https://esm.sh/@supabase/supabase-js@2';const corsHeaders={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};async function materializeSponsoredCandidates(candidates:{campaign_id:string}[],materialize:(campaignId:string)=>Promise<{error:unknown|null}>){for(const candidate of candidates){const result=await materialize(candidate.campaign_id);if(result.error)throw result.error}}
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:corsHeaders});const reply=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{...corsHeaders,'Content-Type':'application/json'}});try{const body=await req.json(),surface=body.surface==='marketplace_search'?'marketplace_search':'marketplace_home',limit=Math.min(Math.max(Number(body.limit)||4,0),8),session=typeof body.session==='string'?body.session:null,category=typeof body.category==='string'?body.category:null;const admin=createClient(Deno.env.get('SUPABASE_URL')??'',Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')??'');const args={p_surface:surface,p_category:category,p_limit:limit,p_session:session};const first=await admin.rpc('fetch_marketplace_sponsored_products_v2',args);if(first.error)throw first.error;await materializeSponsoredCandidates(first.data??[],campaignId=>admin.rpc('materialize_marketplace_ad_campaign_spend',{p_campaign_id:campaignId}));const fresh=await admin.rpc('fetch_marketplace_sponsored_products_v2',args);if(fresh.error)throw fresh.error;return reply({success:true,products:fresh.data??[]})}catch(error){console.error('[marketplace-ads]',error);return reply({success:false,products:[]},503)}});
