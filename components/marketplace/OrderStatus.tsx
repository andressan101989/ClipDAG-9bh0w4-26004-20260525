import React from 'react';import {StyleSheet,Text,View} from 'react-native';import {Colors,Radius,Spacing} from '@/constants/theme';import type {MarketplaceOrderEvent,MarketplaceOrderStatus} from '@/services/marketplaceFulfillmentService';
export const statusLabel=(s:MarketplaceOrderStatus)=>({confirmed:'Confirmado',processing:'En preparación',shipped:'Enviado',delivered:'Entregado',cancelled:'Cancelado',refunded:'Reembolsado',partially_refunded:'Reembolso parcial'}[s]);
export function StatusBadge({status}:{status:MarketplaceOrderStatus}){return <View style={s.badge}><Text style={s.badgeText}>{statusLabel(status)}</Text></View>}
export function OrderTimeline({events}:{events:MarketplaceOrderEvent[]}){const label=(e:string)=>({order_confirmed:'Pedido confirmado',processing_started:'El vendedor comenzó a preparar el pedido',shipment_created:'Pedido enviado',order_shipped:'Pedido enviado',shipment_updated:'Información de seguimiento actualizada'}[e]??'Actualización del pedido');return <View style={s.timeline}>{events.map(e=><View key={e.id} style={s.event}><View style={s.dot}/><View><Text style={s.text}>{label(e.eventType)}</Text><Text style={s.muted}>{new Date(e.createdAt).toLocaleString()}</Text></View></View>)}</View>}
const s=StyleSheet.create({badge:{backgroundColor:Colors.primaryDim,borderRadius:Radius.full,paddingHorizontal:10,paddingVertical:5},badgeText:{color:Colors.primaryLight,fontWeight:'700'},timeline:{gap:Spacing.md},event:{flexDirection:'row',gap:Spacing.sm},dot:{width:10,height:10,borderRadius:5,backgroundColor:Colors.primary,marginTop:5},text:{color:Colors.textPrimary,fontWeight:'600'},muted:{color:Colors.textSecondary,fontSize:12}});
