# LB4-F10-A audit before runtime edits

Base: cc5ac4766d0a6ba4f9a60f738ea3b7fa2ebe537a. Main checkout has the user's four UI edits, .worktrees and an untracked Figma test; untouched. Integration was clean at this SHA. Isolated branch/worktree: codex/lb4-f10-a-battle-runtime-lifecycle, C:/Users/andre/ClipDAG-lb4-f10-a.

## Reconstructed path and findings

1. broadcast handleGoLive (base line 498) calls startLiveSession with the route's streamId, then sets canonical/live. Agora uses that same streamId (295-304). Invitation/acceptance do not call startLiveSession or navigate to another session. No evidence in this checkout proves why the physical device moved from suffix 76b2a650 to 4db8d39a. Metro directory and original logs have been requested; a new Development Build does not establish which source Metro served.
2. LiveBattleControls uses the runtime actions; liveBattleService sends create/respond/start RPCs. Native hook runAction calls applyAuthoritativeBattle. Independently, controller discovery and the two participant-column subscriptions call get_live_battle_state, then applyBattle. Thus application is NOT exclusively local-action-driven. Controller rejects a Battle whose user/session pair does not match; token authority also revalidates the pair.
3. Countdown/active applyBattle schedules canonical reconciliation and invokes relay.start once per attempted version. Actual transport has a queued, generation-guarded native service, one handler and pending-start deduplication. Web .ts files are intentional platform fallbacks, not a parallel native implementation. getEngine and registerBeforeEngineRelease already use useCallback([]), so unstable identities in useAgoraEngine are not demonstrated.
4. The public hook separately subscribes to live_battle_public_states; signals trigger the snapshot RPC (not trusted row payloads), version reduction and a server/monotonic clock anchor. Both screens render this projection and the Stage's one-second interval only interpolates display time. Missing remoteUids controls the placeholder, not mounting. The controller's private runtime snapshot is not the screen's visibility source.
5. **Broken bridge:** controller updatePublicAuthority (base 248) returns when snapshot.battle is null or suspended. It also ignores a newer active/countdown version for the same known nonterminal Battle. Public UI can advance while relay never reconciles. A CHANNEL_ERROR/TIMED_OUT suspends the controller; subsequent public authority cannot recover it. Versioned private signals normally work, but cannot repair this suspended path.
6. **Lost initial projection:** hook's public effect depends only on state/anchor, while the controller is created later when joined exposes the engine. An unchanged projection is never delivered to that new controller, nor reapplied after context recovery clears controller authority. Re-rendering stable callbacks alone does not destroy the controller (green protection).
7. **Stale subscription:** old onError/onSignal closures have no subscription generation check. A queued old callback can suspend a new session's runtime.
8. **Session read failure masquerades as authority:** broadcast poll (base 537-566) ignores the Supabase error and interprets null rows as canonicalLive=false, disabling the public hook (which clears state/clock) and stopping the controller. Concurrent polls for an older opponent also have no generation guard. This reproduces a UI disappearing while the individual LIVE remains open. A confirmed ended session must still fail closed; a network error is not such confirmation.
9. **Posround rehydration:** getOpenLiveBattlesForSession intentionally excludes completed rows. A controller with no snapshot cannot retrieve a completed Battle from discovery, even when its public projection identifies a valid result. It needs to reconcile that projected ID through the existing private RPC, not construct a Battle from client/public fields.
10. PostgreSQL's existing reconciliation/score outcome/series triggers finalize rounds, calculate outcomes, and open the existing 30-second decision window (F5-A line 522). get_live_battle_public_snapshot returns the stored competitive/series projection. F5-A/C2 and C3 corrections retain canonical series/session authority. No local Stage timer clears the Battle; only projection null/disabled/terminal authority changes visibility. The relay policy holds completed rounds while awaiting/rematch_pending with canonical live pairs and a valid server deadline; next rounds transition through the existing relay service. Reject/expiry terminate the series, not live_sessions.
11. Rematch is visible only for completed + participant + clientState available + window not elapsed (Stage base 368). request/accept/reject handlers are wired on broadcast; viewers only see result. F8-B root box-none/zIndex3, visual panels none and interactive panel box-none remain intact. SeriesSingleFlight, stable idempotency ref and TransitionGate already prevent duplicate requests/round transitions.
12. Cleanup already guards most async work. The hook unregisters snapshot and engine guards and disposes the controller. F10-A will additionally guard stale poll/subscription results and eliminate the redundant explicit release immediately before dispose (dispose already releases). No new runtime, transport, scheduler, RPC or table is needed.

## Limits of physical attribution

The absent native `start` log does **not** prove start() was never invoked: it is emitted after await requestCredentials and installHandler (relay base 224-231). The observed absence is also compatible with credential/handler failure. Add development entry and authorization-stage diagnostics, without tokens, to discriminate. No production queries or physical tests are authorized here. Do not claim a verified physical fix or invent which context field failed on the two iPhones.

## Red evidence

Before runtime/source edits: `node --test --test-reporter=tap tests/liveBattlesLb4F10ARuntimeLifecycle.test.mjs` => 18 tests, 9 passed, 9 failed, zero skipped/cancelled/todo. Exact output: red.tap. Tests execute the controller, native hook effect lifecycle, extracted production poll, and series state/single-flight logic. Existing duplicate-start, pair enforcement, completion hold, rematch availability and single-flight tests passed; they are protections, not newly invented defects.

## Architecture inventory

rg executable discovered outside PATH: C:/Users/andre/AppData/Local/OpenAI/Codex/bin/80b8e5c230e43848/rg.exe. Search patterns: new LiveBattleRuntimeController, new LiveBattleRelayService, applyAuthoritativeBattle, completeLiveBattle, useLiveBattleRelayRuntime, subscribeToLiveBattlesForSession, startLiveSession. One native controller construction and relay construction; one host hook consumer. Public and participant-table subscriptions have different contracts (projection/clock vs privileged reconciliation), not duplicate competitive authority. Keep both; bridge validated projection signals to canonical RPC reconciliation. completeLiveBattle export has no application caller; retain public service compatibility instead of unrelated cleanup. No alternate Stage or rematch component replaces the wired Stage. No bulk removal warranted.

## Documentation

Official Supabase skill read. Changelog https://supabase.com/changelog.md fetched with curl after web tool rejected markdown content type. Realtime guidance: https://supabase.com/docs/guides/realtime/postgres-changes (SUBSCRIBED, CHANNEL_ERROR, TIMED_OUT and RLS/publication requirements). Audit uses local migrations only; no Supabase CLI, production SQL, RPC or configuration changes.
