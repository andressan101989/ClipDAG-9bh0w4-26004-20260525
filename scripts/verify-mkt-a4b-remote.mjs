import crypto from'node:crypto';
const PROJECT='aewwdlvbwpczqyvkwvvj',url=`https://${PROJECT}.supabase.co`,service=process.env.MKT_A4A_SERVICE_KEY,anon=process.env.MKT_A4A_ANON_KEY;
if(process.env.SUPABASE_PROJECT_REF!==PROJECT||!service||!anon)throw new Error('linked_project_credentials_required');
const uuid=()=>crypto.randomUUID(),stamp=Date.now().toString(36),password=`A4b-${uuid()}!aA1`,assert=(name,value)=>{if(!value)throw new Error(`assertion_failed:${name}`)},eq=(a,b)=>Math.abs(Number(a)-Number(b))<1e-8,redact=v=>`${String(v).slice(0,8)}…`;
async function request(path,{token=service,method='GET',body,headers={}}={}){const response=await fetch(url+path,{method,headers:{apikey:token===service?service:anon,Authorization:`Bearer ${token}`,'Content-Type':'application/json',...headers},body:body===undefined?undefined:JSON.stringify(body)}),text=await response.text();let data;try{data=text?JSON.parse(text):null}catch{data=text}if(!response.ok)throw Object.assign(new Error(`remote_${response.status}`),{status:response.status,data});return data}
const rpc=(name,body={},token=service)=>request(`/rest/v1/rpc/${name}`,{method:'POST',body,token}),insert=(table,body)=>request(`/rest/v1/${table}`,{method:'POST',body,headers:{Prefer:'return=representation'}}),select=(table,q)=>request(`/rest/v1/${table}?${q}`),edge=(token,body)=>request('/functions/v1/bdag-ledger',{method:'POST',token,body});
const denied=async work=>{try{await work();return false}catch{return true}};
async function user(role){const email=`mkt-a4b-${role}-${stamp}@example.invalid`,created=await request('/auth/v1/admin/users',{method:'POST',body:{email,password,email_confirm:true}}),login=await request('/auth/v1/token?grant_type=password',{method:'POST',token:anon,body:{email,password}});return{id:created.id,email,token:login.access_token}}
async function account(userId){return rpc('ensure_ledger_account',{p_user_id:userId})}
async function balance(id){return Number((await select('ledger_accounts',`select=balance&id=eq.${id}`))[0].balance)}
async function fund(userId,amount,reference){const destination=await account(userId),platform=(await select('ledger_accounts','select=id&owner_id=is.null&account_type=eq.platform&currency=eq.BDAG&limit=1'))[0].id,tx=uuid();await insert('financial_transactions',{id:tx,from_account_id:platform,to_account_id:destination,operation_type:'marketplace_test_funding',amount,fee_amount:0,currency:'BDAG',status:'completed',reference_type:'marketplace_test_fixture',reference_id:reference,idempotency_key:`a4b-${tx}`,initiated_by:userId});await rpc('ledger_debit',{p_txn_id:tx,p_account_id:platform,p_amount:amount,p_description:'Dedicated MKT-A4B fixture funding',p_metadata:{fixture:'mkt-a4b'}});await rpc('ledger_credit',{p_txn_id:tx,p_account_id:destination,p_amount:amount,p_description:'Dedicated MKT-A4B fixture funding',p_metadata:{fixture:'mkt-a4b'}});return destination}
async function snapshot(accounts){return{buyer:await balance(accounts.buyer),escrow:await balance(accounts.escrow),sellerA:await balance(accounts.sellerA),hostB:await balance(accounts.hostB),platform:await balance(accounts.platform)}}
async function buy({pin,variant,token,session}){const address={recipient_name:'A4B Test Buyer',line1:'Dedicated fixture address',line2:null,city:'Test City',region:'Test Region',postal_code:'00000',country:'US',phone:null},reservation=await rpc('create_live_marketplace_checkout_reservation',{p_session_id:session,p_live_session_product_id:pin,p_variant_id:variant,p_quantity:1,p_shipping_address:address,p_idempotency_key:uuid()},token),checkout=reservation.checkout.id,order=reservation.orders[0].id,key=uuid(),receipt=await edge(token,{action:'marketplace_checkout_pay',checkout_id:checkout,idempotency_key:key}),retry=await edge(token,{action:'marketplace_checkout_pay',checkout_id:checkout,idempotency_key:key});assert('payment_retry_same',JSON.stringify(receipt)===JSON.stringify(retry));return{checkout,order,receipt}}

const sellerA=await user('seller-a'),hostB=await user('host-b'),buyerC=await user('buyer-c'),ids={storeA:uuid(),storeB:uuid(),productA:uuid(),productB:uuid(),variantA:uuid(),variantB:uuid(),session:uuid()};
await request('/rest/v1/user_profiles?on_conflict=id',{method:'POST',body:[sellerA,hostB,buyerC].map((u,i)=>({id:u.id,email:u.email,username:`a4b_${i}_${stamp}`,display_name:['Seller A','Creator B','Buyer C'][i]})),headers:{Prefer:'resolution=merge-duplicates,return=minimal'}});
for(const u of[sellerA,hostB])await insert('marketplace_sellers',{user_id:u.id,status:'approved',display_name:u===sellerA?'Seller A':'Creator B',approved_at:new Date().toISOString()});
await insert('marketplace_stores',[{id:ids.storeA,seller_id:sellerA.id,name:'A4B Seller Store',slug:`a4b-seller-${stamp}`,status:'active'},{id:ids.storeB,seller_id:hostB.id,name:'A4B Creator Store',slug:`a4b-creator-${stamp}`,status:'active'}]);
const products=[{id:ids.productA,seller_id:sellerA.id,store_id:ids.storeA,title:'Affiliate LIVE Product',description:'Dedicated fixture',price:1,currency:'BDAG',category:'physical',stock:5,status:'active',category_id:'10000000-0000-4000-8000-000000000002',product_type:'physical',moderation_status:'approved',published_at:new Date().toISOString()},{id:ids.productB,seller_id:hostB.id,store_id:ids.storeB,title:'Own LIVE Product',description:'Dedicated fixture',price:1,currency:'BDAG',category:'physical',stock:5,status:'active',category_id:'10000000-0000-4000-8000-000000000002',product_type:'physical',moderation_status:'approved',published_at:new Date().toISOString()}];
await insert('products',products);const skuA=`A4B-A-${stamp}`.toUpperCase(),skuB=`A4B-B-${stamp}`.toUpperCase();await insert('marketplace_product_variants',[{id:ids.variantA,product_id:ids.productA,store_id:ids.storeA,seller_id:sellerA.id,sku:skuA,sku_normalized:skuA,title:'Default',price:1,status:'active',is_default:true,combination_key:''},{id:ids.variantB,product_id:ids.productB,store_id:ids.storeB,seller_id:hostB.id,sku:skuB,sku_normalized:skuB,title:'Default',price:1,status:'active',is_default:true,combination_key:''}]);await insert('marketplace_inventory_levels',[{variant_id:ids.variantA,on_hand:5,reserved:0},{variant_id:ids.variantB,on_hand:5,reserved:0}]);
await rpc('start_live_session',{p_session_id:ids.session,p_title:'A4B Affiliate Proof'},hostB.token);const offer=await rpc('upsert_my_live_affiliate_offer',{p_product_id:ids.productA,p_offer_scope:'specific_creator',p_creator_id:hostB.id,p_commission_bps:500,p_status:'active'},sellerA.token),ownPin=await rpc('pin_live_session_product',{p_session_id:ids.session,p_product_id:ids.productB,p_featured_variant_id:ids.variantB,p_idempotency_key:uuid()},hostB.token),affiliatePin=await rpc('pin_live_session_product',{p_session_id:ids.session,p_product_id:ids.productA,p_featured_variant_id:ids.variantA,p_idempotency_key:uuid()},hostB.token);assert('own_pin',ownPin.commerce_mode==='own_product'&&ownPin.creator_commission_bps===0);assert('affiliate_pin',affiliatePin.commerce_mode==='affiliate_product'&&affiliatePin.creator_commission_bps===500);
const security={viewerPin:await denied(()=>rpc('pin_live_session_product',{p_session_id:ids.session,p_product_id:ids.productA,p_featured_variant_id:null,p_idempotency_key:uuid()},buyerC.token)),anonymousPin:await denied(()=>rpc('pin_live_session_product',{p_session_id:ids.session,p_product_id:ids.productA,p_featured_variant_id:null,p_idempotency_key:uuid()},anon)),directOfferInsert:await denied(()=>request('/rest/v1/marketplace_live_affiliate_offers',{method:'POST',token:buyerC.token,body:{}})),directPinInsert:await denied(()=>request('/rest/v1/live_session_products',{method:'POST',token:buyerC.token,body:{}})),directPinUpdate:await denied(()=>request(`/rest/v1/live_session_products?id=eq.${ownPin.id}`,{method:'PATCH',token:buyerC.token,body:{position:9}})),directPinDelete:await denied(()=>request(`/rest/v1/live_session_products?id=eq.${ownPin.id}`,{method:'DELETE',token:buyerC.token}))};assert('command_security',Object.values(security).every(Boolean));
const accounts={buyer:await fund(buyerC.id,5,ids.productA),sellerA:await account(sellerA.id),hostB:await account(hostB.id),platform:(await select('ledger_accounts','select=id&owner_id=is.null&account_type=eq.platform&currency=eq.BDAG&limit=1'))[0].id,escrow:(await select('ledger_accounts','select=id&owner_id=is.null&account_type=eq.marketplace_escrow&currency=eq.BDAG&limit=1'))[0].id};
const beforeOwn=await snapshot(accounts),own=await buy({pin:ownPin.id,variant:ids.variantB,token:buyerC.token,session:ids.session}),afterOwn=await snapshot(accounts),ownAllocation=(await select('marketplace_payment_allocations',`select=*&order_id=eq.${own.order}`))[0];assert('own_split',eq(ownAllocation.gross_amount,1)&&eq(ownAllocation.seller_net_amount,.9)&&eq(ownAllocation.platform_fee_amount,.1)&&eq(ownAllocation.creator_commission_amount,0)&&ownAllocation.creator_user_id===null);assert('own_payment_balances',eq(beforeOwn.buyer-afterOwn.buyer,1)&&eq(afterOwn.escrow-beforeOwn.escrow,1)&&eq(afterOwn.hostB,beforeOwn.hostB)&&eq(afterOwn.platform,beforeOwn.platform));
const beforeAffiliate=await snapshot(accounts),affiliate=await buy({pin:affiliatePin.id,variant:ids.variantA,token:buyerC.token,session:ids.session}),afterAffiliate=await snapshot(accounts),allocation=(await select('marketplace_payment_allocations',`select=*&order_id=eq.${affiliate.order}`))[0],source=(await select('marketplace_live_commission_sources',`select=*&order_id=eq.${affiliate.order}`))[0],purchase=(await select('live_commerce_purchase_events',`select=*&order_id=eq.${affiliate.order}`))[0];assert('affiliate_split',eq(allocation.gross_amount,1)&&eq(allocation.seller_net_amount,.85)&&eq(allocation.creator_commission_amount,.05)&&eq(allocation.platform_fee_amount,.1)&&allocation.creator_user_id===hostB.id);assert('affiliate_source',source.host_id===hostB.id&&source.seller_id===sellerA.id&&source.affiliate_offer_id===offer.id&&source.creator_commission_bps===500&&eq(source.creator_commission_amount,.05));assert('purchase_event',purchase.host_id===hostB.id&&purchase.order_id===affiliate.order&&eq(purchase.creator_commission_amount,.05));assert('affiliate_payment_balances',eq(beforeAffiliate.buyer-afterAffiliate.buyer,1)&&eq(afterAffiliate.escrow-beforeAffiliate.escrow,1)&&eq(afterAffiliate.sellerA,beforeAffiliate.sellerA)&&eq(afterAffiliate.hostB,beforeAffiliate.hostB)&&eq(afterAffiliate.platform,beforeAffiliate.platform));
security.directSourceRead=await denied(()=>request('/rest/v1/marketplace_live_commission_sources?select=*',{token:buyerC.token}));security.directSourceWrite=await denied(()=>request('/rest/v1/marketplace_live_commission_sources',{method:'POST',token:buyerC.token,body:{}}));security.viewerPurchaseFeed=(await request(`/rest/v1/live_commerce_purchase_events?select=*&session_id=eq.${ids.session}`,{token:buyerC.token})).length===0;assert('financial_source_security',security.directSourceRead&&security.directSourceWrite&&security.viewerPurchaseFeed);
await rpc('seller_start_marketplace_order_processing',{p_order_id:affiliate.order,p_idempotency_key:uuid()},sellerA.token);await rpc('seller_ship_marketplace_order',{p_order_id:affiliate.order,p_carrier_name:'A4B Carrier',p_service_level:'Test',p_tracking_number:`A4B-${stamp}`,p_tracking_url:null,p_seller_note:'Dedicated fixture',p_idempotency_key:uuid()},sellerA.token);const beforeDelivery=await snapshot(accounts),deliveryKey=uuid(),delivery=await edge(buyerC.token,{action:'marketplace_order_confirm_delivery',order_id:affiliate.order,idempotency_key:deliveryKey}),afterDelivery=await snapshot(accounts),deliveryRetry=await edge(buyerC.token,{action:'marketplace_order_confirm_delivery',order_id:affiliate.order,idempotency_key:deliveryKey}),afterRetry=await snapshot(accounts);assert('delivery_split',eq(beforeDelivery.escrow-afterDelivery.escrow,1)&&eq(afterDelivery.sellerA-beforeDelivery.sellerA,.85)&&eq(afterDelivery.hostB-beforeDelivery.hostB,.05)&&eq(afterDelivery.platform-beforeDelivery.platform,.1));assert('delivery_retry',delivery.data?.settlement?.id===deliveryRetry.data?.settlement?.id||delivery.settlement?.id===deliveryRetry.settlement?.id);assert('delivery_retry_balances',JSON.stringify(afterDelivery)===JSON.stringify(afterRetry));const legs=await select('marketplace_settlement_legs',`select=leg_type,amount&settlement_id=eq.${delivery.data?.settlement?.id??delivery.settlement?.id}`);assert('three_legs',legs.length===3&&new Set(legs.map(x=>x.leg_type)).size===3);
const paymentReconciliation=await rpc('reconcile_marketplace_payments'),settlementReconciliation=await rpc('reconcile_marketplace_settlements'),commissionReconciliation=await rpc('reconcile_marketplace_live_commissions');for(const[k,v]of Object.entries(commissionReconciliation))assert(`commission_reconciliation_${k}`,Number(v)===0);assert('payment_reconciliation',paymentReconciliation.escrow_shortfall===0&&paymentReconciliation.confirmed_state_mismatches===0);assert('settlement_reconciliation',settlementReconciliation.escrow_difference===0&&settlementReconciliation.escrow_shortage===0&&settlementReconciliation.escrow_surplus===0);
console.log(JSON.stringify({project:PROJECT,ids:{sellerA:redact(sellerA.id),hostB:redact(hostB.id),buyerC:redact(buyerC.id),session:redact(ids.session),offer:redact(offer.id),ownOrder:redact(own.order),affiliateOrder:redact(affiliate.order)},security,own:{before:beforeOwn,after:afterOwn,allocation:ownAllocation,purchaseEvent:true},affiliate:{beforePayment:beforeAffiliate,afterPayment:afterAffiliate,allocation,source:{mode:source.commerce_mode,bps:source.creator_commission_bps,amount:source.creator_commission_amount},beforeDelivery,afterDelivery,legs},reconciliation:{paymentReconciliation,settlementReconciliation,commissionReconciliation}},null,2));
