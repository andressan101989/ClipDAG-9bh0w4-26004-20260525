import test from 'node:test';import assert from 'node:assert/strict';import{readFileSync}from'node:fs';
const screen=readFileSync('app/create-product.tsx','utf8'),service=readFileSync('services/marketplaceService.ts','utf8'),sql=readFileSync('supabase/migrations/20260805100000_unblock_marketplace_product_publication.sql','utf8');
test('shipping loading is authoritative before publication',()=>{assert.match(screen,/type ShippingState = 'loading'\|'ready'\|'empty'\|'error'/);assert.match(screen,/shippingState === 'loading' \|\| shippingState === 'error'/);assert.match(screen,/Crear perfil de envío/)});
test('every publication stage emits safe diagnostics',()=>{for(const marker of['publish_started','draft_create_start','draft_create_success','variant_configuration_start','variant_configuration_success','shipping_assignment_start','shipping_assignment_success','publication_rpc_start','publication_rpc_success','affiliate_configuration_start','affiliate_configuration_success','publication_failed'])assert.match(screen,new RegExp(marker));assert.match(screen,/productFingerprint/);assert.doesNotMatch(screen,/console\.(log|warn).*description|console\.(log|warn).*token/i)});
test('known publication failures retain exact Spanish messages',()=>{for(const value of['Agrega al menos una imagen válida.','El perfil de envío no pertenece a esta tienda.','No tienes permiso para publicar este producto.','Ese SKU ya existe en tu tienda.'])assert.match(service,new RegExp(value));assert.match(service,/normalizeMarketplacePublicationError/)});
test('checked publication preserves all readiness gates',()=>{for(const value of['marketplace_seller_not_approved','marketplace_store_inactive','marketplace_product_media_required','marketplace_product_not_ready_shipping_incomplete','marketplace_product_not_ready_no_active_variant','marketplace_product_not_ready_inventory_not_configured','marketplace_product_not_ready_out_of_stock'])assert.match(sql,new RegExp(value));assert.match(sql,/for update/);assert.match(sql,/status='active'/)});
test('client preflights authoritative state and preserves the draft',()=>{assert.match(screen,/evaluateMarketplaceProductPublication/);assert.match(screen,/let activeDraftId = draftProductId/);assert.doesNotMatch(screen,/\.from\('products'\)\.(update|insert)/)});
